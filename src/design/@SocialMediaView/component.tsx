import styled from 'styled-components';
import {motion} from 'framer-motion';
const SocialMediaView = styled(motion.div)`
 position: relative;
 top: 0;
 left: 0;
 display: flex;
 justify-content: center;
 align-items: center;
 padding: .25rem;
 gap: .5rem;

`;

export default SocialMediaView;